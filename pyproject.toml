[project]
name = "rnts"
version = "0.1.0"
description = "a build tool written in Python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.17",
]
